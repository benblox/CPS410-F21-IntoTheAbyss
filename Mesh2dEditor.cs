using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

public class Mesh2dEditor : EditorWindow
{
    [MenuItem("Window/Mesh2dEditor")]

    public static void ShowWindow()
    {
        EditorWindow.GetWindow(typeof(Mesh2dEditor));
    }
    private void OnGUI()
    {
        

        
    }
}